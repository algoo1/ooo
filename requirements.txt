# RunPod handler
runpod==1.6.2

# Core ML (PyTorch already in base image)
transformers==4.35.0
tokenizers==0.14.1

# Image processing
Pillow==10.0.1
requests==2.31.0

# Performance optimization
accelerate==0.24.1
